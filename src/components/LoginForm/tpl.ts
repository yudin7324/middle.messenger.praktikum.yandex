const tpl = `
  <h1 class="login__title">{{title}}</h1>
  {{{ loginTextField }}}
  {{{ passwordTextField }}}
  <div class="login__buttons">  
    {{{ button }}}
    {{{ link }}}
  </div>
`;

export default tpl;
